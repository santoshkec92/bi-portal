apiVersion: v1
kind: Service
metadata:
  name: bi-portal
  namespace: bi-portal
  labels:
    app.kubernetes.io/name: bi-portal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bi-portal
  ports:
    - name: http
      port: 80
      targetPort: 8000
