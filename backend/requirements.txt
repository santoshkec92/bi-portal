# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# --- Settings / validation ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Persistence (portal metadata) ---
SQLAlchemy==2.0.36
# Postgres driver is optional; SQLite ships with Python and is the default.
psycopg2-binary==2.9.10

# --- Auth: Okta OAuth2 / OIDC (Authorization Code + PKCE) ---
authlib==1.3.2
httpx==0.28.1
itsdangerous==2.2.0          # signed session cookies
python-jose[cryptography]==3.3.0  # JWT (id_token) decode/verify

# --- Dashboard data sources ---
anthropic==0.40.0            # Claude API
snowflake-connector-python==3.12.4  # optional; only used when SNOWFLAKE_* is set

# --- Dev / test ---
pytest==8.3.4
